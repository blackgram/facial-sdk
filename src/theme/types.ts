export interface LivenessTheme {
  primary: string;
  primaryPressed?: string;
  success: string;
  danger: string;
  warning: string;
  background: string;
  backgroundSecondary: string;
  text: string;
  textSecondary: string;
  textMuted: string;
  border: string;
  overlay: string;
  borderRadius: number;
}
